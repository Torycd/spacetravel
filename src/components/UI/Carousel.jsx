// import PropTypes from 'prop-types';

const Carousel = ({slides}) => {
  return (
    <div>
      {slides.map((s) => (
        <div key={slides.name}>

        </div>
      ))}
    </div>
  )
}



export default Carousel
